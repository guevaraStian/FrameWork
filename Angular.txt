Algunos comandos que se usa en el framework angular, y en la mayoria de casos, el "--" explica para que sirve el codigo.

//NPM
npm install
npm init
npm update
npm restart  --reiniciar
npm start  --Se utiliza para iniciar un paquete cuando es necesario.
npm stop  --Se utiliza para detener la ejecuci�n de un paquete.
npm version  --Muestra la versi�n actual de npm instalada en tu ordenador.
npm publish

npm init
Package name: restapisnode
author: SebasGS
npm install --save express
npm install --save-dev nodemon   //monitorea los cambios del lado del servidor sin tener que reiniciarlo
npm start
npm install --save mongoose
npm install --save-dev nodemon
npm install --save shortid multer //molter
npm install --cache

//NVM
nvm install node  --Instalar la �ltima versi�n de node:
nvm ls-remote   --Ver todas las versiones de node disponibles para instalar:
nvm install [version]   --Instalar una versi�n espec�fica:
nvm use <version>   --Utilizar una versi�n espec�fica:
nvm uninstall [version] --Desinstalar una versi�n espec�fica:
nvm alias default [version]   --Establecer una versi�n de node por defecto
nvm ls  -- Listar todas las versiones de node instaladas:

//PM2
pm2 start [archivo] -- este es uno de los comandos PM2 que inicia una aplicaci�n de Node.js.
pm2 startup -- configura PM2 para que se inicie autom�ticamente en el arranque del sistema.
pm2 stop [all] -- detiene todas las aplicaciones administradas por PM2.
pm2 delete [all] -- elimina todas las aplicaciones del gestor.
pm2 reload all -- recarga todas las aplicaciones en PM2 sin tiempo de inactividad.
pm2 ecosystem -- este es uno de los comandos PM2 que crea un archivo de configuraci�n de ecosystem para tus aplicaciones.
pm2 logs --  muestra los registros de todas las aplicaciones en vivo.
pm2 logs app -- muestra los registros en vivo de una aplicaci�n espec�fica.
pm2 logs app - lines 100 -- limita los registros mostrados a las �ltimas 100 l�neas.
pm2 logs - json -- muestra los registros en formato JSON.
pm2 logs - format -- personaliza el formato de los registros.
pm2 list - watch -- muestra una lista de aplicaciones y actualiza autom�ticamente.
pm2 monit -- monitorea el uso de recursos y el estado de tus aplicaciones.










